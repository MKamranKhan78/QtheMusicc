<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View" />

        <variable
            name="click"
            type="com.techswivel.qthemusic.customData.interfaces.AdapterOnClick" />

        <variable
            name="obj"
            type="com.techswivel.qthemusic.models.Payment" />

    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/_10dp"
        android:background="@color/transparent_color"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/paymentTypeTextviewID"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_margin="@dimen/_5dp"
            android:layout_centerVertical="true"
            android:layout_marginStart="@dimen/_10dp"
            android:fontFamily="@font/manrope_bold"
            android:text="@{obj.payment}"
            android:textColor="@color/color_white_text_profile"
            android:textSize="@dimen/_14sp" />

        <ImageView
            android:id="@+id/imageviewTick"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:visibility="@{obj.selectedButtonVisibility}"
            android:layout_marginEnd="@dimen/_20dp"
            android:src="@drawable/ic_icon_feather_check" />

    </RelativeLayout>
</layout>